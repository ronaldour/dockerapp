pipeline {
  agent any

  stages{
    stage('Deploy'){
      steps{
        echo "Deploying"
        sh "aws --version"
      }
    }

  }
  post {
    failure{
      echo "fail"
    }
    success{
      echo "Success"
    }
  }
}
